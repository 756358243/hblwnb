/**
 * 
 */
package com.vkl.hblw.controller.user.setting;

import org.apache.log4j.Logger;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * @author xcc 系统设置界面相关Controller
 */
@Controller
@RequestMapping(value = "/user/setting")
public class SettingController {

	private static final Logger log = Logger.getLogger(SettingController.class);

	private String ret(Model model, String view) {
		return "/user/setting/" + view;
	}

}
